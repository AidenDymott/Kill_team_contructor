### To run the project please download all files, open the project folder, open an intergrated terminal and then use the command
python manage.py migrate
### Followed by
python manage.py runserver
